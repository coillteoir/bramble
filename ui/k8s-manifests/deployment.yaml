apiVersion: apps/v1
kind: Deployment
metadata:
  creationTimestamp: null
  labels:
    app: ui
  name: ui
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ui
  strategy: {}
  template:
    metadata:
      creationTimestamp: null
      labels:
        app: ui
    spec:
      containers:
      - image: davidlynchsd/bramble-ui
        name: bramble-ui
        ports:
        - containerPort: 5555
          protocol: TCP
      serviceAccountName: ui-sa
