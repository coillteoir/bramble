apiVersion: pipelines.bramble.dev/v1alpha1
kind: Pipeline
metadata:
  name: simple-test
spec:
  tasks:
    - name: root
      spec:
        image: ubuntu
        command:
        - sleep
        - "10"
