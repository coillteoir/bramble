---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.11.1
  creationTimestamp: null
  name: executions.pipelines.bramble.dev
spec:
  group: pipelines.bramble.dev
  names:
    kind: Execution
    listKind: ExecutionList
    plural: executions
    singular: execution
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Execution is the Schema for the executions API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: ExecutionSpec defines the desired state of Execution
            properties:
              pipeline:
                description: Reference to the pipeline which will be executed
                type: string
              repo:
                description: Git repo hosting the code to be tested against pipeline
                type: string
            required:
            - pipeline
            - repo
            type: object
          status:
            description: ExecutionStatus defines the observed state of Execution
            properties:
              completed:
                description: States if the pipeline is completed
                type: boolean
              completedTasks:
                description: Tasks which have already completed
                items:
                  type: string
                type: array
              error:
                description: States if the pipeline has failed at any point
                type: boolean
              executing:
                description: Describes which strings are currently running
                items:
                  type: string
                type: array
            required:
            - completed
            - completedTasks
            - error
            - executing
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
